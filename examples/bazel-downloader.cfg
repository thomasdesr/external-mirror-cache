# Bazel downloader config for external-mirror-cache
# Usage: bazel build --experimental_downloader_config=/path/to/bazel-downloader.cfg //...

# Rewrite all HTTP/HTTPS URLs through the caching proxy
rewrite ([^/]+)/(.+) localhost:8443/$2/$3
